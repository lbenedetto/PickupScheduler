import 'package:flutter/material.dart';

class PickupsScreen extends StatelessWidget{
	@override
	Widget build(BuildContext context){
		return new Scaffold(
			appBar: new AppBar(
				title: new Text('Pickups'),
			),
			body: new Text("hi"),
		);
	}
}